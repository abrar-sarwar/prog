# Runtime dependencies for prog.
# Pinned to versions confirmed working on Python 3.14 / macOS.

discord.py==2.7.1
SQLAlchemy==2.0.49
asyncpg==0.31.0
alembic==1.18.4
python-dotenv==1.2.2

# --- Dev only (test runner; required to run the Checkpoint 3 suite) ---
pytest==8.3.4
